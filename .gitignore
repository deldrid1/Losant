# Temporary file system files
.DS_Store

# Logs
logs
*.log
npm-debug.log*

# Runtime data
pids
*.pid
*.seed

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directory
# https://docs.npmjs.com/misc/faq#should-i-check-my-node-modules-folder-into-git
node_modules

# Used by our linter
*.bundled

# Used by Builder to cache external files for 24 hours
.builder-cache

# Used by impt to cache authentication information locally
.impt.auth*

# exclude any compiled files (they should be set as tagged releases)
build

# exclude the temp folder for saving trash files to (ElectricImp-Sublime makes you save all files at every deploy which can become obnoxious - just save them to /temp and its not an issue)
temp
github.conf

# Exclude our secrets File
.impt.project.secrets
